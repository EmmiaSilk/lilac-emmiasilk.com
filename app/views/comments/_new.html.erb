<div class="comment-form">
  <%= form_for([@article, @article.comments.build]) do |form| %>
    <p>
      <%= form.text_area :body %>
    </p>
    <div class="button-container">
      <%= form.button "Submit" %>
    </div>
  <% end %>
</div>
